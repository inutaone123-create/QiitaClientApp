version: '3.8'

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace:cached
    working_dir: /workspace
    stdin_open: true
    tty: true
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    command: sleep infinity
